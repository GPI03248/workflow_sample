# API: cfd.mesh_geometry

Geometry helpers (delegated to structured.py).

Currently the mesh is always uniform Cartesian.  If curved / unstructured
grids are added later, geometry-specific helpers would go here.

## Extension Notes

See docs/cfd_iteration_guide.md for how to extend this module.
